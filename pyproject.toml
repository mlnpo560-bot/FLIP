[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "flip-money"
version = "0.1.0"
description = "FLIP decentralized monetary protocol research prototype"
requires-python = ">=3.11"
dependencies = ["cryptography==46.0.2"]
